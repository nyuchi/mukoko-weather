/**
 * Icon registry shim — per the Mzizi doctrine, components import icons from
 * `@/lib/icons` (the nyuchi-icons registry surface), never from lucide-react
 * directly. Add icons here as components need them.
 */
export { ExternalLink, Moon, Sun, BookOpen, LogOut } from "lucide-react";
